# U Link 当前进度交接

更新时间：2026-08-26

## 项目状态

`U Link` 已从产品需求确认和第一版原型阶段进入前期开发准备阶段，已初始化后台和小程序两个基础工程。

已完成基础项目文档：

- `AGENTS.md`：项目协作规则、设计方向和工作约束。
- `DEVELOPMENT_PRINCIPLES.md`：项目开发原则，覆盖真实性、数据完整性、产品建模、持久化、测试、调试、API 合同、部署和分支纪律。
- `README.md`：项目定位、MVP 范围和下一步。
- `docs/product.md`：MVP 产品范围、学生认证、指导员审核、会员与内容方向。
- `docs/product-modules.md`：产品模块图口径，后续首页和内容入口以此为准。
- `docs/architecture.md`：腾讯云自部署 + PayloadCMS + PostgreSQL + COS 的第一版架构方向。

已完成基础工程：

- `package.json` / `pnpm-workspace.yaml`：pnpm workspace 根目录配置。
- `apps/cms`：PayloadCMS + Next.js 后台/API 工程，已配置 PostgreSQL adapter。
- `apps/miniprogram`：原生微信小程序工程骨架，当前使用 mock 数据。
- `docker-compose.yml`：本地 PostgreSQL 开发服务。
- `apps/cms/.env.example`：后台环境变量示例，不含真实密钥。

当前已通过：

- `pnpm install`
- `pnpm typecheck`
- `pnpm --filter @ulink/cms test`
- `pnpm --filter @ulink/cms build`

## 已确认产品方向

- 产品名称：`U Link`。
- 载体：微信小程序。
- 长期定位：从校园场景出发，逐步连接职业规划、实习实践、金融职业资源、本土文化内容和国际交流机会。
- MVP 目标：
  - 学生可以微信登录并完成注册认证。
  - 学生可以加入会员并通过微信支付购买。
  - 先做好金融系列职业内容发布和服务入口，后续再扩展生态。
- 第一版落地学校：先以广东金融学院新生为首批试点，但产品不能表现为“给广东金融学院定制”的小程序。
- 所有学校、学院、专业、班级相关设计都按多学校模型处理；第一版只是后台数据里先只有一个学校。

## 技术方向

- 小程序端优先使用原生微信小程序。
- 后台第一版倾向使用自部署 PayloadCMS。
- 部署优先放在腾讯云。
- 数据库优先 PostgreSQL。
- 文件资源使用腾讯云 COS。
- 生产环境必须保证持久化：数据库、上传文件和密钥都不能依赖应用容器本地磁盘，重新部署 PayloadCMS/Next.js 时不能清空业务数据。
- 第一版可以先把学生登录、会员、订单、支付、内容发布和学校数据编辑逻辑集中在 PayloadCMS 项目里，后续业务复杂后再拆独立服务。
- 不优先使用微信云开发作为主数据库。

## 当前开发进展

- 2026-08-26：开始实现小程序与后台第一批真实业务流。
  - 后台新增 `/api/miniprogram/*` API 命名空间，覆盖登录、资料提交、认证状态、首页、学校数据、内容列表/详情、成长计划、订单创建、订单查询和本地模拟支付回调。
  - 后台新增可测试服务层，覆盖 session 签名验证、微信登录抽象、学生资料提交、成长计划订单创建和支付回调幂等处理。
  - 后台新增 `memberships` 和 `payment-events` collections，用于持久化会员状态和支付事件，避免支付回调重复处理。
  - 小程序 API 层已从纯本地 mock 改为 `wx.request` 调用后台；mock fallback 只在显式 demo mode 下启用。
  - 小程序新增登录页和资料完善页，首页、成长计划、内容详情、认证状态和我的页开始接后端形状数据。
  - 当前微信登录和微信支付仍是本地开发 gateway，后续需要接真实 AppID/AppSecret、商户号、证书和回调验签。
- 2026-08-26：补充指导员认证管理第一批实现。
  - 后台新增指导员审核服务层，按当前账号手机号匹配班级 `instructorPhones`，只允许查看和操作负责班级学生。
  - 后台新增 `/api/miniprogram/instructor/verifications`，支持获取待认证学生、批量确认通过或标记需确认。
  - 小程序新增 `pages/instructor-verifications` 管理页；“我的”页仅在后端判断有管理权限时显示“学生认证管理”入口。
  - 首页在有待认证学生时显示轻量待办入口；无待办时不显示。
  - 当前指导员手机号来源仍依赖已登录账号的手机号，后续接微信手机号授权/短信验证后继续沿用该权限匹配规则。
- 2026-08-26：把开发原则纳入项目入口，并开始按原则收紧登录与资料提交链路。
  - `README.md` 已增加开发原则入口，明确 mock 只能用于本地开发/原型/demo，生产不能用假数据掩盖真实接口失败。
  - 根目录新增 `pnpm verify`，用于集中执行后台测试、类型检查和 lint。
  - 后台开始接真实微信登录配置：配置 `WECHAT_MINIPROGRAM_APP_ID` 和 `WECHAT_MINIPROGRAM_APP_SECRET` 后，登录 gateway 会调用微信 `code2Session`；未配置时必须显式开启本地 mock。
  - 后台新增微信手机号授权服务与 `/api/miniprogram/phone/wechat`，资料提交必须带服务端签发的手机号验证 token，不能只靠前端手填手机号。
  - 小程序资料页改为先微信授权手机号，再提交学生资料；本地 demo fallback 仍只在显式 demo mode 下使用。
- 2026-08-26：补充真实微信支付下单适配器第一步。
  - 后台支付 gateway 已支持微信支付 V3 JSAPI/小程序下单：服务端按商户号、证书序列号和商户私钥生成请求签名，调用 `/v3/pay/transactions/jsapi` 获取 `prepay_id`，再生成小程序 `wx.requestPayment` 所需参数。
  - `apps/cms/.env.example` 已补充 `WECHAT_PAY_MCH_ID`、`WECHAT_PAY_CERT_SERIAL_NO`、`WECHAT_PAY_PRIVATE_KEY` 和 `WECHAT_PAY_NOTIFY_URL` 占位配置，不提交真实密钥。
  - 小程序成长计划页真实支付后不直接伪造“已加入”，而是回查订单和会员状态；本地 mock 支付仍只在显式 mock 开关开启后可用。
- 2026-08-26：补充真实微信支付回调处理。
  - 后台新增 `/api/miniprogram/payments/wechat-callback`，使用原始 body 和 `Wechatpay-*` 请求头进行微信支付回调验签。
  - 回调解密使用 `WECHAT_PAY_API_V3_KEY` 对 `resource` 做 AES-256-GCM 解密，并校验 AppID、商户号、订单号和金额后再写入支付事件、更新订单、激活会员。
  - `apps/cms/.env.example` 已补充 `WECHAT_PAY_API_V3_KEY`、`WECHAT_PAY_PLATFORM_SERIAL_NO` 和 `WECHAT_PAY_PLATFORM_PUBLIC_KEY` 占位配置，不提交真实密钥。
  - 真实生产支付仍需要微信商户平台证书/公钥配置、回调域名配置和微信支付联调验证。
- 2026-08-26：补充信息发布到小程序模块页的第一批闭环。
  - 后台内容读取会过滤未发布内容和已停用分类，避免后台关闭的栏目继续出现在学生端。
  - PayloadCMS richText 正文会转换成小程序可直接展示的文本，避免详情页出现对象内容。
  - 小程序首页四个成长模块已可点击，进入对应模块内容列表，列表内容来自 `/api/miniprogram/content?module=...`。
  - 模块列表和内容详情页会展示分类、时间/类型、摘要、标签和正文；暂无内容时展示真实空状态，不用假内容掩盖后台未发布。
- 2026-08-26：补充内容运营字段，支持活动、机会和服务入口更像真实发布内容。
  - PayloadCMS 内容新增开放状态、会员专属、行动按钮文案、行动链接、名额上限和已预约人数。
  - 小程序内容列表和详情页会展示报名状态、会员专属标记、已预约/剩余名额和标签。
  - 内容详情页行动按钮先支持复制外部链接；若后台没有配置链接，则显示真实“暂未开放”，不做假跳转。
- 2026-08-26：补充会员专属内容访问规则。
  - 内容列表仍可展示会员专属内容的标题、摘要、状态和名额信息，方便学生理解权益。
  - 内容详情接口会根据登录学生的有效会员状态决定是否返回完整正文和行动链接。
  - 未登录、未开通或会员已过期时，会员专属详情只返回锁定提示和“开通成长计划”入口，不暴露报名链接或完整正文。
- 2026-08-26：补充短信手机号认证基础链路。
  - 后台新增短信验证码 challenge 持久化模型，验证码只保存哈希、过期时间、尝试次数和使用时间，不保存明文。
  - 后台新增 `/api/miniprogram/phone/sms/request` 和 `/api/miniprogram/phone/sms/verify`，短信验证成功后签发与微信手机号授权共用的 `phoneVerificationToken`。
  - 小程序资料页新增“微信授权 / 短信验证”互斥入口；短信方式填写手机号和验证码，验证成功后才允许提交资料。
  - 生产短信服务供应商仍待确认；当前仅提供显式本地 mock 开关，不能在生产用 mock 短信冒充真实验证。

## 学生注册认证需求

首次登录必须完成注册认证，不能跳过。

学生流程：

1. 微信登录。
2. 二选一完成手机号认证：
   - 微信授权手机号。
   - 短信验证手机号。
3. 补全真实姓名、性别、生日、学校、学院、专业、班级。
4. 勾选用户协议和隐私政策。
5. 提交资料后进入“资料已提交”页。
6. 可进入首页，状态为待认证，等待对应指导员确认。

关键规则：

- 微信授权手机号和短信验证手机号是互斥的两种认证方式。
- 如果选择短信验证，不应再显示微信手机号。
- 如果选择微信授权，需要明确完成授权后才能提交。
- 学校、学院、专业、班级必须从后台数据中选择，不允许学生手填。
- 真实姓名要求填写真实姓名，但第一版不做强身份校验。
- 真实姓名提示应温和，表达“用于活动签到、会员权益核销和必要身份确认”，不使用恐吓式文案。
- 提交后学生可先进入首页，不因待认证状态阻塞基础浏览。

## 指导员认证管理需求

指导员参与学生资料确认，但流程保持轻量。

- 指导员由 PayloadCMS 后台配置，可以在批量导入班级时一起填写指导员手机号。
- 一个班级可配置多个指导员，一个指导员可负责多个班级。
- 指导员用微信登录并验证手机号后，按手机号匹配可管理班级。
- 指导员入口放在“我的”页，仅指导员可见。
- 如果有待认证学生，首页也可以显示轻量入口和待认证数量。
- 指导员认证管理页支持：
  - 状态筛选。
  - 班级筛选。
  - 查看学生姓名、手机号、学院、专业、班级、提交时间、状态。
  - 批量确认。
  - 单个标记“需确认”。
- 第一版确认或标记无需强制填写备注。

## 消息提醒

- 第一版暂不接服务号通知。
- 使用小程序订阅消息作为增强提醒，不作为流程依赖。
- 指导员进入审批页时可以主动订阅待认证提醒。
- 学生提交成功页可以主动订阅认证结果通知。
- 指导员确认通过或标记需确认时，两种结果都可以发送订阅消息。

## 原型文件

当前主要原型：

- `prototype/student-auth/index.html`
  - 独立 HTML 页面。
  - 用于展示学生注册认证、提交成功、首页、我的页、指导员审批入口和审批页。
  - 已嵌入 U Link logo，不依赖外部图片。
  - 当前用于给客户看第一版学生认证主链路。

历史原型：

- `prototype/register/`
  - 早期欢迎页和登录页原型。
- `prototype/membership/`
  - 会员卡和后台配置方向的早期静态原型。

## 当前原型已完成

`prototype/student-auth/index.html` 已包含以下屏幕：

- 欢迎页 / 微信登录。
- 学生补全资料页。
- 资料已提交页。
- 首页状态。
- 我的页面。
- 指导员审批页。

最近已修正的问题：

- 首页不再暗示 U Link 是广东金融学院专属小程序。
- 注册资料页不再提前显示“当前认证学校”。
- 学校仍必须选择，虽然第一版只有广东金融学院一个选项。
- 性别控件下方大空白已修复。
- 手机号认证改为互斥的两种方式：
  - 微信授权：只显示微信手机号授权入口，授权后显示已授权号码。
  - 短信验证：只显示手机号输入、验证码输入和获取验证码按钮。
- 手机号认证的两段分段控件已修复，不再出现右侧空白。
- 提交成功页已加入学校、学院、专业、班级和指导员信息。
- 首页列表排版已调整为标准 iOS 列表结构：左图标、左文字、右箭头。
- 首页已按产品模块图调整为“成长服务模块优先，角色状态提示为辅”：
  - 首页不再保留单独的大标题介绍区，避免与模块卡和认证状态重复。
  - 首页首个卡片改为角色状态卡：
    - 学生视角：显示“请等待认证完成”，提示资料已提交并等待指导员确认。
    - 指导员视角：有待认证学生时显示待认证人数并可进入审批；无待办时不显示。
  - 已加入确认后的核心模块：职业规划、实习实践、金融底色、文化交流。
  - 模块摘要分别为：
    - 职业规划：测评、规划、简历与课程支持。
    - 实习实践：实训营、岗位介绍与实践机会。
    - 金融底色：金融沙龙、财商课与机构资源。
    - 文化交流：非遗文化、中外交流与研学路线。
  - 四个核心模块已补点击后的详情页：
    - 职业规划：测评、规划、简历、课程支持。
    - 实习实践：实训营、岗位介绍、实践机会。
    - 金融底色：金融沙龙、财商课、机构资源。
    - 文化交流：非遗文化、中外交流、研学路线。
  - 职业规划中的测评和简历不做 U Link 内部系统，只用服务方页面占位图表示未来接入位置。
  - 规划、课程支持、实训营、岗位介绍、实践机会、金融沙龙、财商课、机构资源、非遗文化、中外交流和研学路线，已从抽象专题说明改为新闻/头条式假内容。
  - 模块页内容现在包含栏目、标题、时间与类型、摘要、标签、阅读量、预约人数或剩余名额，便于用户理解“这是什么、能不能参加、下一步做什么”。
  - 模块页每张内容卡片已支持点击进入详情；详情页展示标题、时间类型、摘要、标签、状态、下一步和正文段落。
  - 自有内容后续由 PayloadCMS 配置展示；学生端不出现后台、供应商、协作类内部说明或编辑部署名。
  - 详情页只使用已确认模块内容，不额外扩写未确认栏目。
  - 已加入常用入口：咖啡空间、规划咨询、活动信息。
  - 首页底部已移除重复的“我的认证状态”和“学生认证管理”入口；“我的”页只保留个人信息、协议与隐私等设置项。
  - “我的”页设置列表已改为独立 `settings-list` 布局，避免复用首页三列入口样式导致文字竖排或数字徽标拉伸。
  - 首页已加入“友邻成长计划”原型示意：`¥5 / 半年`，点击进入详情页，说明有效期内接收实习邀请、活动提醒、成长内容推送和咨询预约提醒；当前只做模拟加入，不接真实支付。
- 学生端首页不出现“第三方协作”这类运营视角文字。
- 已更新 `prototype/student-auth/verify-home-content.mjs`，用于检查首页模块文案、成长计划入口、内容详情入口和禁用文案。

## 设计要求

后续所有原型和小程序页面继续坚持 Apple iOS 风格：

- 浅色系统背景。
- 白色分组卡片。
- 细分割线。
- 清晰层级。
- 蓝色主操作按钮。
- 克制圆角。
- 表单、列表和个人页尽量接近 iOS grouped form / inset grouped list。

交付前必须主动做截图自检，重点检查：

- 分段控件列数是否与选项数量一致。
- 列表项是否左对齐，图标、文字、箭头是否稳定。
- 按钮文字是否溢出。
- 表单字段是否出现异常空白。
- 底部固定按钮或 tabbar 是否遮挡内容。
- 页面是否错误暗示“只为某一个学校定制”。

## 待继续确认

- 根据产品模块图继续细化职业规划、实习实践、金融底色、文化交流的 MVP 首批内容和开放状态。
- 会员套餐、价格、权益、有效期。
- 微信支付商户号和结算主体。
- 短信服务供应商，腾讯云短信签名和模板。
- 小程序订阅消息模板。
- 学校、学院、专业、班级批量导入格式。
- PayloadCMS 数据模型和 API 设计。
- 腾讯云部署形态：CVM、轻量应用服务器或容器化服务。
- 生产持久化方案：PostgreSQL 使用腾讯云数据库还是自建持久化云硬盘，COS bucket 和备份策略如何配置。
- 域名、备案、微信小程序合法请求域名。

## 建议下一步

下一轮建议从第一版可交付原型继续：

1. 继续检查并打磨 `prototype/student-auth/index.html` 的完整视觉和交互。
2. 确认学生注册认证原型是否可以作为客户沟通版本。
3. 补第一版首页内容模块，体现“金融职业生态”而不是单纯注册工具。
4. 再进入 PayloadCMS 数据模型设计。
5. 最后再初始化正式微信小程序和后端项目。
